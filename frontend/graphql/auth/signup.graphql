mutation Signup(
    $username: String!
    $email: String!
    $password: String!
) {
    signup(newUser: {
        username: $username
        email: $email
        password: $password
    }) {
        status
    }
}